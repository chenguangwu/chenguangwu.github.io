#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""为 design 分类 15 个精选工具补真实 features/steps/tips，供 gen_guide_pages.py 生成高质量指南（§4.4）。

背景：gen_guide_pages.py 的 derive() 在缺 features/steps/tips 时回退用「适用场景」当核心功能/技巧、
用「示例标题」当使用步骤，导致指南页模板化（it / general / finance 均已踩过）。本脚本按各工具真实
功能补写，之后重跑 gen_guide_pages.py --industry design 生成正式指南页。

选材原则（§4.4 克制）：只覆盖专业度高且高频的「计算 / 换算 / 校验」类工具，不覆盖纯生成器类：
- 单位与尺寸换算 7：px↔rem、px→vw、像素↔英寸(DPI)、冲印尺寸、间距阶梯、字号阶梯、宽高比
- 色度学 1：WCAG 对比度检查（同主题的 color-contrast-check 已有指南，contrast-checker 为重复工具，均不再新增）
- 摄影光学 4：色温、景深/超焦距、等效焦距与视角、安全快门
- 存储与配色 3：照片存储容量、调色板生成
跨分类重名 slug（audio-recorder / image-compress / image-mosaic / image-to-base64 / image-watermark）
暂不纳入，避免与既有 guides/<slug>-guide.html 互覆（如需可后续用 --prefix design- 处理）。

用法：
  python3 scripts/fix_design_guide_fields.py --dry-run
  python3 scripts/fix_design_guide_fields.py --apply

写入格式：indent=1 / ensure_ascii=False（仓库规范，避免全文件重排）。
_build.py 只读 title/scenarios/examples/faqs，新增字段不影响构建。
"""
import argparse
import json
import os

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
DD = os.path.join(ROOT, "i18n", "tools", "content_deepdive.json")

F = {
    # ── 单位与尺寸换算类（7）──────────────────────────────────
    "px-to-rem": {
        "features": [
            "按根字号把 px 值换算为 rem（rem = px ÷ 根字号）",
            "根字号可自定义，默认 16px，适配改过 html font-size 的项目",
            "结果保留 4 位小数并回显当前 1rem 等于多少 px",
            "纯本地计算，不上传任何输入",
        ],
        "steps": [
            "在「像素值」输入需要换算的 px 数值",
            "确认或修改「根字号」，多数浏览器默认为 16px",
            "读取结果区的 rem 值（保留 4 位小数）",
            "把 rem 值填入 CSS，替换原本写死的 px",
            "如需反向核对，改用 rem 转 px 工具复算",
        ],
        "tips": [
            "若 html 用 font-size: 62.5% 把根字号改成 10px，换算结果会整体变化，务必先确认实际根字号",
            "rem 相对根元素、em 相对父元素，混用容易算错层级，建议全站统一用 rem",
            "用户可在浏览器调大默认字号，rem 会随之缩放，这正是它利于无障碍的原因",
            "边框、1px 描边等不需要随字号缩放的细节建议保留 px",
        ],
    },
    "rem-to-px": {
        "features": [
            "按根字号把 rem 值反算为 px（px = rem × 根字号）",
            "根字号可自定义，便于对应不同项目的 html font-size 设置",
            "结果保留 2 位小数，直接给出渲染后的实际像素值",
            "纯本地计算，不上传任何输入",
        ],
        "steps": [
            "在「rem 值」输入设计稿或代码中的 rem 数值",
            "填入当前项目的根字号（默认 16px）",
            "读取结果区的 px 值",
            "与设计稿标注或 DevTools 的 computed 值对照",
            "若不一致，回到项目确认 html 的 font-size 是否被覆盖",
        ],
        "tips": [
            "调试布局时用 px 核对真实渲染尺寸最直观，rem 换算只是手段",
            "浏览器默认根字号 16px，但用户可能改过，对不上先查这一项",
            "设计稿若以 750px 宽度出图，rem 换算还需考虑设计稿缩放比，不能只按 16px 折算",
            "结果保留 2 位小数，亚像素差异属正常渲染行为",
        ],
    },
    "vh-vw": {
        "features": [
            "在视口单位（vw / vh）与 px 之间双向换算",
            "支持按视口宽度或高度为基准，四种模式（px→vw、px→vh、vw→px、vh→px）",
            "基准视口尺寸可自定义（默认 1920px），便于对齐设计稿断点",
            "纯本地计算，不上传任何输入",
        ],
        "steps": [
            "选择转换模式（px → vw / px → vh / vw → px / vh → px）",
            "输入需要换算的数值",
            "填入基准视口尺寸（按模式对应宽度或高度）",
            "读取结果区带单位的值与所用基准提示",
            "把结果代入 CSS 并用 DevTools 改变窗口宽度验证表现",
        ],
        "tips": [
            "移动端浏览器地址栏收起/展开会让 vh 变化，关键全屏布局优先用 dvh / svh / lvh",
            "vw 会包含滚动条宽度，桌面端可能出现横向溢出，可用 100% 或 calc 规避",
            "视口单位适合全屏区块与流式字号，组件内部间距仍建议 rem 更可控",
            "换算只在给定基准下成立，改断点后需按新基准重算",
        ],
    },
    "image-dpi-converter": {
        "features": [
            "在像素尺寸与物理尺寸（英寸/厘米）之间双向换算",
            "按屏幕对角线计算实际 PPI",
            "三个页签分别处理像素→物理、物理→像素、屏幕 PPI",
            "纯本地计算，不上传任何输入",
        ],
        "steps": [
            "选择换算方向（像素 → 物理尺寸 / 物理尺寸 → 像素 / 屏幕 PPI）",
            "输入像素值或物理尺寸，并填写 DPI / PPI",
            "读取结果区的物理尺寸与宽高比",
            "印刷前核对输出尺寸是否满足要求",
            "屏幕 PPI 场景填入对角线与分辨率即可",
        ],
        "tips": [
            "DPI 只决定打印尺寸下的像素密度，屏幕上显示多大只由像素数决定",
            "「DPI」与「PPI」在图像文件中常被混用，像素/英寸才是判断依据",
            "网页图片标注 72DPI 或 300DPI 不影响显示大小，改的只是元数据",
            "印刷厂要求的有效 DPI 通常指图片实际像素 ÷ 成品尺寸，务必按成品尺寸核",
        ],
    },
    "photo-print-size": {
        "features": [
            "按照片像素与打印 DPI 计算可输出的最大物理尺寸（厘米）",
            "对照常见相纸规格（一寸、二寸、3R、4R 等）提示像素是否充足",
            "给出各规格所需的最小像素与总像素量",
            "纯本地计算，不上传任何输入",
        ],
        "steps": [
            "输入照片的宽、高像素",
            "填写期望的打印 DPI（手持照片通常 300）",
            "读取最大可打印尺寸（如 33.9 × 25.4 cm）",
            "在规格表中核对目标相纸是否标注像素充足",
            "像素不足时降低 DPI 或改用更小的输出规格",
        ],
        "tips": [
            "DPI 越低打印尺寸越大但近看颗粒越明显，墙面大幅输出可放宽、手持照片建议 ≥300",
            "英寸 × DPI = 像素，想保证清晰应先定 DPI 再反推所需像素",
            "3:2 的相机原图打印 6 寸（4R）会被裁边，构图上留出余量",
            "打印尺寸还受相纸裁切与出血影响，实际成品可能略小于计算值",
        ],
    },
    "spacing-scale": {
        "features": [
            "按基数与比例生成等比间距阶梯（如 4、5、6.3、7.8…px）",
            "层级数可调（5–15 级），同步输出 CSS 变量",
            "以条形长度直观对比各档间距的相对大小",
            "纯本地计算，不上传任何输入",
        ],
        "steps": [
            "填写基础间距（常用 4 或 8）",
            "设定比例（如 1.25）",
            "调整层级数确定阶梯长度",
            "查看各档数值与条状对比",
            "复制生成的 CSS 变量粘到项目 :root",
        ],
        "tips": [
            "间距 token 建议只取整数或 0.5 的倍数，否则写进 CSS 后难以对齐像素",
            "基数与控制组件高度对齐（如按钮 40px 用 8 的倍数）整体更协调",
            "需要重叠或负外边距时直接写负值，不要借间距 token 表达负号",
            "阶梯比例不宜过大，超过 1.5 时相邻档位跨度会明显失衡",
        ],
    },
    "typography-scale": {
        "features": [
            "按基数与比例生成等比字号阶梯（display 到 h6 共 7 级）",
            "每级同时给出字号与示例文字预览",
            "可指定字体族并导出 CSS 变量",
            "纯本地计算，不上传任何输入",
        ],
        "steps": [
            "填写基础字号（正文常用 16px）",
            "设定比例（1.2 紧凑 / 1.25 通用 / 1.333 舒展）",
            "选择字体族以便预览真实观感",
            "核对各级字号与层级落差是否合理",
            "复制 CSS 变量用于项目",
        ],
        "tips": [
            "比例越小层级越平缓，1.2 适合内容密集的后台，1.333 以上适合营销页",
            "正文低于 16px 在移动端可读性会下降，基字号不宜过小",
            "相邻两级字号差小于 15% 时视觉层级不明显，容易分不清标题层次",
            "行高需随字号调整，大字号的行高比应减小，否则标题会显得松散",
        ],
    },
    "photo-aspect-ratio-calculator": {
        "features": [
            "由宽高像素计算最简宽高比（如 1920×1080 → 16:9）",
            "提供常见比例预设（1:1、4:3、3:2、16:9、2.35:1 等）一键套用",
            "反算指定比例下另一边的对应长度，并给出 300DPI 打印尺寸",
            "纯本地计算，不上传任何输入",
        ],
        "steps": [
            "输入图片的宽与高像素",
            "读取约简后的宽高比与小数比值",
            "或点选预设比例套用到目标尺寸",
            "按比例反算另一边长度，用于裁剪框设定",
            "核对 300DPI 下的打印尺寸是否满足用途",
        ],
        "tips": [
            "比例换算只做裁剪不拉伸，取框时务必保持原比例以免画面变形",
            "同一题材不同平台要求不同（社媒多裁切、打印多留白），导出前统一按目标比例预裁",
            "3:2 与 16:9 差异明显，相机原图发横版视频封面通常需两侧裁切",
            "约简后的最简比便于命名归档，小数比值便于程序计算",
        ],
    },

    # ── 色度学（1）────────────────────────────────────────────
    "checker": {
        "features": [
            "按 WCAG 2.1 计算前景色与背景色的对比度比率",
            "同时判定 AA / AAA 在正常文字与大号文字下的四档合规结果",
            "支持取色器与手输 HEX，并可一键交换前景与背景",
            "纯本地计算，不上传任何输入",
        ],
        "steps": [
            "输入前景色（文字色）与背景色，可用取色器或直接填 HEX",
            "读取对比度比值（如 14.68:1）",
            "核对 AA（正文 ≥4.5:1）与 AAA（正文 ≥7:1）是否通过",
            "若未达标，调整明度后重新取值直到通过",
            "如需反色方案，点击交换按钮再核一次",
        ],
        "tips": [
            "大号文字（≥18px 或 ≥14px 粗体）阈值放宽到 3:1（AA）与 4.5:1（AAA），不要一律按 4.5 卡",
            "对比度只算相对亮度，相同的比值不代表观感一致，红色与蓝色的可读性差异仍需实测",
            "半透明文字需先与底色合成实色再计算，直接拿 RGBA 值算会偏高",
            "装饰性文字、禁用态与 Logo 不受 WCAG 对比度要求约束",
        ],
    },

    # ── 摄影光学（4）──────────────────────────────────────────
    "color-temperature-converter": {
        "features": [
            "在开尔文色温（K）与 RGB / HEX 之间双向转换",
            "给出色温描述（烛光 / 日光 / 阴天等）与相机白平衡参考预设",
            "提供色温滑杆，可连续拖动观察颜色变化",
            "纯本地计算，不上传任何输入",
        ],
        "steps": [
            "输入色温值（K）或直接填入 RGB",
            "用滑杆细调直到颜色符合预期",
            "读取对应的 RGB / HEX 与白平衡说明",
            "把 HEX 用于设计稿，或把 K 值作为拍摄白平衡参考",
            "需要反向取值时切换 RGB 输入模式",
        ],
        "tips": [
            "低色温（如 2700K）偏暖黄、高色温（如 9000K）偏冷蓝，与直觉相反，勿记反",
            "转换基于经验近似式，得到的是屏幕白点参考值，不用于色彩科学级的精确测量",
            "相机白平衡预设与屏幕色温并非一一对应，实际拍摄仍以灰卡与实测为准",
            "显示器自身色温会叠加影响观感，校色后的屏幕才有参考意义",
        ],
    },
    "depth-of-field-calculator": {
        "features": [
            "按光圈、焦距、对焦距离与传感器格式计算前景深、后景深与总景深",
            "给出超焦距与近/远清晰界限（以米为单位）",
            "传感器格式内置对应弥散圆，切换即重算",
            "纯本地计算，不上传任何输入",
        ],
        "steps": [
            "选择传感器格式（决定弥散圆 c）",
            "输入焦距、光圈值与对焦距离",
            "读取近点 / 远点 / 总景深与超焦距",
            "按想虚化或想全清晰调整光圈后重算对比",
            "拍摄时按算得的距离设置对焦",
        ],
        "tips": [
            "弥散圆按传感器与输出尺寸取经验值（全画幅常用 0.03mm），不同标准下结果会有差异",
            "后景深约为前者的两倍，对焦位置宜略偏向背景一侧",
            "当对焦距离超过超焦距时远点为无限远，风光拍摄对焦到超焦距可最大化清晰范围",
            "缩小光圈会增加景深，但也可能因衍射降低锐度，f/8–f/11 常是平衡点",
        ],
    },
    "focal-length-equivalent": {
        "features": [
            "在任意两个画幅之间换算等效焦距（乘以裁切系数）",
            "按全画幅基准换算出视角范围（水平 × 垂直）",
            "列出 APS-C、M4/3 等常见画幅的等效焦距对照",
            "纯本地计算，不上传任何输入",
        ],
        "steps": [
            "选择源画幅与目标画幅",
            "输入原始焦距，或用「全画幅焦距」栏反向对照",
            "读取等效焦距与视角角度",
            "在下方对照表中比较各画幅的成像范围",
            "选镜头时按等效视角评估构图习惯",
        ],
        "tips": [
            "裁切系数只影响视角，不改变景深公式中的实际焦距，等效景深需另行折算",
            "APS-C 分佳能 1.6x 与尼康/索尼/富士 1.5x，不要一律按 1.5 算",
            "等效焦距用于跨画幅比视角很方便，但透视由拍摄距离决定，与画幅无关",
            "手机标注的等效焦距即已折算过，无需再乘系数",
        ],
    },
    "shutter-speed-calculator": {
        "features": [
            "按焦距与传感器裁切系数估算手持拍摄的安全快门速度",
            "计入镜头防抖等级（IS 档位）给出可放慢的档数",
            "列出从 1/1000 到数秒的快门速度适用场景参考",
            "纯本地计算，不上传任何输入",
        ],
        "steps": [
            "输入镜头焦距",
            "选择传感器裁切系数",
            "选择镜头防抖等级（无防抖选 0）",
            "读取建议的安全快门档位",
            "按参考表核对拍摄题材所需的快门速度",
        ],
        "tips": [
            "安全快门约为 1/焦距 秒，裁切画幅需先乘系数再取倒数",
            "防抖可放慢数档但依赖手持稳定性，标称档数不代表每次都稳",
            "拍摄运动主体时快门受凝固动作约束，与手抖无关，应另按题材提速",
            "长曝光即便有防抖也应使用三脚架，并关闭防抖避免反向抖动",
        ],
    },

    # ── 存储与配色（3）────────────────────────────────────────
    "photo-storage-calculator": {
        "features": [
            "按相机像素、图片格式与位深估算单张照片体积",
            "按拍摄数量折算总占用并对照各容量存储卡可存张数",
            "支持视频时长占用的粗算",
            "纯本地计算，不上传任何输入",
        ],
        "steps": [
            "选择相机像素与图片格式（含 RAW 与压缩类型）",
            "设定位深度与计划拍摄数量",
            "输入存储卡容量",
            "读取单张体积、总量与可存张数",
            "在格式对照表中比较不同格式的体积差异",
        ],
        "tips": [
            "RAW 保留未压缩或轻度压缩的原始数据，体积通常数倍于 JPG，出行前按 RAW 规划容量",
            "实际可存张数低于理论值，文件系统与碎片会占用少量空间",
            "连拍与高感场景下 RAW+JPG 双存会显著增加占用，需按需开启",
            "存储卡勿拍到写满再换，留 10% 余量有利于写入稳定",
        ],
    },
    "color-palette-generator": {
        "features": [
            "由主色按配色规则（类似 / 互补 / 三角等）生成协调调色板",
            "每个色块给出 HEX 与 RGB 色值，并可一键复制",
            "支持导出 CSS 变量与渐变代码",
            "纯本地计算，不上传任何输入",
        ],
        "steps": [
            "设定主色（取色器或输入色值）",
            "选择配色规则与色块数量",
            "查看生成的整套调色板",
            "微调个别色块直到层次协调",
            "复制 CSS 变量或渐变代码用于项目",
        ],
        "tips": [
            "类似色柔和统一、互补色对比强烈，按画面需要的张力选择",
            "生成后仍需用对比度工具核验正文与背景，配色协调不代表文字可读",
            "建议为主色预留明暗两侧的固定档位（如 100–900），便于统一按钮与状态色",
            "品牌色不要直接大面积铺底，降低饱和度或提高明度后更耐看",
        ],
    },
}


def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--dry-run", action="store_true")
    ap.add_argument("--apply", action="store_true")
    a = ap.parse_args()
    if not (a.dry_run or a.apply):
        print("请指定 --dry-run 或 --apply")
        return 2

    dd = json.load(open(DD, encoding="utf-8"))
    added = 0
    for slug, fields in F.items():
        key = "design/" + slug
        if key not in dd:
            print("!! 缺条目:", key)
            continue
        for k, v in fields.items():
            dd[key][k] = v
        added += 1
    print("待写入工具数: %d；字段: %s" % (added, " ".join(sorted({k for f in F.values() for k in f}))))
    bad = [k for k, f in F.items()
           if len(f.get("features", [])) != 4 or len(f.get("steps", [])) != 5 or len(f.get("tips", [])) != 4]
    if bad:
        print("!! 条数不符合 features=4/steps=5/tips=4 的:", bad)

    if a.dry_run:
        for key in list(F)[:2]:
            print("\n预览 design/%s => %s"
                  % (key, json.dumps({k: F[key][k] for k in ("features", "steps", "tips")}, ensure_ascii=False)[:400]))
        return 0

    json.dump(dd, open(DD, "w", encoding="utf-8"), ensure_ascii=False, indent=1)
    print("已写入 content_deepdive.json")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
