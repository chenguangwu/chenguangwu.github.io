#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""批次 13 deep-dive 校正。

1) calc-1：原示例用「平均加速度 a≈2P/(m·v)」单段法，与页面重建后的两段模型（扭矩段 + 功率段）
   口径不一致，按页面默认算例重写。
2) voltage-2：原条目为「48V 轻混系统与电路压降」，与页面（12V 发电机电压调节 + 线路压降）不符，
   整条按页面口径重写。

仓库规范：content_deepdive.json 使用 indent=1，必须保持一致，避免全量重排噪音。
"""
import json
import os

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
DD_PATH = os.path.join(ROOT, 'i18n/tools/content_deepdive.json')


def main():
    with open(DD_PATH, encoding='utf-8') as f:
        d = json.load(f)

    # ---------------- 1. calc-1：示例改为两段模型算例 ----------------
    c1 = d['automotive/calc-1']
    c1['examples'] = [{
        'title': '两段模型估算 0–100 km/h',
        'body': (
            '整车质量 1500 kg、最大功率 130 kW、峰值扭矩 250 N·m、一档总传动比 12、轮胎滚动半径 0.32 m、'
            '传动效率 88%、风阻 CdA 0.65、前驱、附着系数 0.9 时：轮端功率 = 130 × 0.88 = 114.4 kW，'
            '一档轮端最大驱动力 = 250 × 12 × 0.88 ÷ 0.32 = 8250 N。'
            '扭矩段 0~49.9 km/h、加速度约 4.86 m/s²（受前驱附着上限约束）用时 2.86 s；'
            '功率段 49.9~100 km/h 用时 4.09 s；两次换挡损失 0.7 s，合计约 7.65 s，'
            '平均加速度 3.63 m/s²，比功率 86.7 kW/t。若附着系数降到 0.5，附着上限降至 2.70 m/s²，'
            '同样的动力估算结果会拉长到约 9.94 s。'
        ),
    }]

    # ---------------- 2. voltage-2：整条重写 ----------------
    v2 = d['automotive/voltage-2']
    v2['title'] = '发电机电压调节与线路压降'
    v2['scenarios'] = [
        '充电指示灯偶亮或电瓶频繁亏电时，核算调节器目标电压与实际充电电流是否正常。',
        '加装大功率用电设备后核对供电线路压降，避免末端电压过低导致设备工作异常。',
        '更换发电机或电压调节器后，按车型标准核对调节电压区间是否正确。',
    ]
    v2['examples'] = [{
        'title': '调节电压与线路压降核算',
        'body': (
            '系统标称 12 V、调节器目标 14.4 V、切入转速 700 rpm（按发动机转速折算）、当前转速 2500 rpm 时，'
            '发电机已进入满输出区，实际输出电压 14.4 V；电池静态电压 12.4 V、充电回路总电阻 0.08 Ω，'
            '则充电电流 = (14.4 − 12.4) ÷ 0.08 = 25.0 A。'
            '供电线路长 5 m（单程）、线径 2.5 mm²、铜导线电阻率 0.0175 Ω·mm²/m，'
            '单线电阻 = 0.0175 × 5 ÷ 2.5 = 0.035 Ω，往返双线 0.07 Ω；'
            '负载 10 A 时压降 = 10 × 0.07 = 0.70 V（约 5.8%），末端电压仅 11.30 V。'
            '一般建议线路压降控制在 3% 以内，否则应加大线径或缩短走线。'
        ),
    }]
    v2['faqs'] = [
        {'q': '汽车发电机的调节电压多少算正常？',
         'a': '多数 12 V 系统在发动机运转、中等转速且负载不大时，蓄电池两端电压在 13.8~14.4 V 之间属正常，'
              '部分车型为照顾电池寿命会控制在 13.5~14.0 V，冬季或亏电后可能短时升到 14.5~14.8 V。'
              '低于 13.2 V 往往充不满，高于 15.0 V 则有过度充电与电解液失水风险，两者都应检查调节器与搭铁。'},
        {'q': '为什么怠速时充电电流很小甚至为负？',
         'a': '发电机输出与转速相关，低于切入转速时输出不足以覆盖整车用电，缺口由蓄电池补充，此时电流表读数接近零或为负。'
              '怠速时发电机通常只能提供 30%~50% 的额定电流，因此长时间怠速开着大灯、空调与音响会净放电瓶电量。'},
        {'q': '线路压降多少算过大？',
         'a': '一般建议整车供电线路在额定负载下压降不超过 3%（约 0.4 V），大灯、起动机等大电流回路可放宽到 5%。'
              '压降偏大常见于线径偏细、接头氧化、搭铁点锈蚀或走线过长。排查时应在带载状态下测量，'
              '空载测量的电压无法反映真实压降。'},
    ]

    with open(DD_PATH, 'w', encoding='utf-8') as f:
        json.dump(d, f, ensure_ascii=False, indent=1)
        f.write('\n')
    print('已校正 2 条（calc-1 示例校准 + voltage-2 整条重写）')


if __name__ == '__main__':
    main()
