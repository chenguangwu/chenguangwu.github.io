#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""ai 分类的「📐 计算公式 / 原理」数据（供通用 scripts/fix_formula.py 消费）。

条目形态：
  {'eq': '公式', 'desc': '依据/口径说明'}

覆盖 56 个计算类页面（2026-09-13 审计）。判定口径：type="number" 输入 ≥2 视为计算类。
豁免 8 个交互 demo（ai-code-review / ai-prompt-generator / ai-text-summarizer /
image-classification / ocr / sentiment-analysis / speech-to-text / text-summarization），
它们处理文本或图像、无数值公式，且已自带 formula-box。
公式均逐条对照页面 `function calc()` 实现撰写，确保与代码一致。
"""
TITLE_CALC = '📐 计算公式 / 原理'

MAP = {
    # ---- 分类评估 ----
    'ai': {
        'eq': '准确率 = (TP+TN)/(TP+FP+FN+TN)×100%；精确率 = TP/(TP+FP)×100%；'
              '召回率 = TP/(TP+FN)×100%；F1 = 2TP/(2TP+FP+FN)×100%',
        'desc': '由混淆矩阵四类计数推导：准确率衡量整体判对比例，精确率关注"报为正的有多少真"，'
                '召回率关注"真正例被找出多少"，F1 是二者调和平均。样本极度不平衡时准确率会失真，'
                '应改看 F1 或 PR 曲线下的面积。',
    },
    'model-accuracy': {
        'eq': '准确率 = (TP+TN)/(TP+TN+FP+FN)×100%',
        'desc': '分类模型最直观的整体判对比例。它隐含"各类代价相同"的假设，当负样本占绝大多数时'
                '（如 99:1 的欺诈检测），恒判负类即可拿到 99% 准确率却毫无价值，'
                '此时必须配合精确率、召回率或 MCC 一起判读。',
    },
    'precision-recall': {
        'eq': '精确率 P = TP/(TP+FP)；召回率 R = TP/(TP+FN)；F1 = 2PR/(P+R)',
        'desc': 'P 与 R 天然此消彼长：放宽阈值召回升高但误报增多。F1 用调和平均综合两者，'
                '对较小的一方更敏感（接近 0 时 F1 也接近 0），适合作为单一权衡指标；'
                '若业务更看重漏检（如疾病筛查）应改用 F2 加权召回。',
    },
    'specificity': {
        'eq': '特异度 TNR = TN/(TN+FP)×100%；假阳性率 FPR = FP/(TN+FP)×100% = 1 − 特异度',
        'desc': '特异度衡量负类被正确排除的能力，与召回率（灵敏度）构成 ROC 曲线的两个轴。'
                '筛查场景宁可牺牲特异度换取高召回，而 spam 误杀正常邮件的代价很高，'
                '应反过来优先保特异度。',
    },
    'ai-7': {
        'eq': 'MCC = (TP·TN − FP·FN)/√[(TP+FP)(TP+FN)(TN+FP)(TN+FN)]；'
              'FPR = FP/(FP+TN)；TNR = TN/(FP+TN)；FDR = FP/(TP+FP)',
        'desc': 'MCC 同时用到混淆矩阵四格，取值 [−1,1]，0 表示与随机猜测相当，'
                '在不平衡数据上比准确率稳健。FDR 回答"报为正的结果里有多少是误报"，'
                '与精确率互补（P = 1 − FDR），常用于多重假设检验的校正判读。',
    },
    'cohens-kappa': {
        'eq': 'Po = (TP+TN)/N；Pe = [(TP+FP)(TP+FN) + (TN+FN)(TN+FP)]/N²；κ = (Po − Pe)/(1 − Pe)',
        'desc': 'Cohen\'s Kappa 剔除"偶然一致"后衡量两个标注者（或模型与金标）的真实一致性：'
                'Pe 是按各自边缘分布随机撞上的概率。κ>0.8 视为几乎完全一致，0.4–0.6 中等，'
                '低于 0 说明一致性还不如随机，标注口径需要重新对齐。',
    },
    'auc-rank': {
        'eq': 'U = ΣRank(正例) − nPos(nPos+1)/2；AUC = U/(nPos × nNeg)',
        'desc': 'Mann-Whitney U 统计视角：AUC 等于"随机抽一个正例与一个负例，正例得分更高的概率"。'
                '把所有样本按得分升序排名，用正例秩和减去最小可能秩和即得 U，'
                '再除以正负配对总数 nPos×nNeg，结果 0.5 表示无区分能力。',
    },
    'roc-auc': {
        'eq': 'AUC ≈ 0.5 + 0.5·erf[(μ₊ − μ₋)/√(2(σ₊² + σ₋²))]；'
              "分离度 d' = (μ₊ − μ₋)/√[(σ₊² + σ₋²)/2]",
        'desc': '在正负样本得分各自近似正态的假设下，AUC 可由两分布的均值差与方差直接算出，'
                '无需逐对比较，适合只有汇总统计量的快速估算。若真实分布长尾或严重偏态，'
                '该近似会偏离实测 AUC，此时应改用秩次法。',
    },
    'rmse': {
        'eq': 'MSE = (Σyᵢ² − 2Σyᵢŷᵢ + Σŷᵢ²)/n；RMSE = √MSE；MAE = |Σyᵢ − Σŷᵢ|/n；R² = 1 − SSres/SStot',
        'desc': '平方和展开式可在只有汇总量时算 MSE，避免逐样本遍历。RMSE 与 y 同量纲但对离群点敏感，'
                'MAE 更稳健；R² 表示相对"直接预测均值"提升的比例，可为负，'
                '说明模型还不如基准线。',
    },

    # ---- 向量与距离 ----
    'ai-4': {
        'eq': '欧氏距离 = √[Σ(aᵢ − bᵢ)²]；曼哈顿距离 = Σ|aᵢ − bᵢ|；'
              '余弦相似度 = (a·b)/(‖a‖·‖b‖)',
        'desc': '三种常用向量度量并列给出：欧氏看绝对位置差，受量纲影响需先标准化；'
                '曼哈顿按轴累加，在高维稀疏场景更稳；余弦只看夹角、忽略模长，'
                '因此常用于 embedding 与文本相似度比较。',
    },
    'cosine-similarity': {
        'eq': 'cos θ = (Σaᵢbᵢ) / (√Σaᵢ² · √Σbᵢ²)',
        'desc': '余弦相似度取值范围 [−1,1]，1 为同向、0 为正交、−1 为反向。'
                '因为做了模长归一化，它对向量整体缩放不敏感，适合比较长度差异很大的文本或特征向量；'
                '若需要同时反映模长差异应改回欧氏距离。',
    },
    'ai-14': {
        'eq': 'cos(q, d) = (q·d)/(‖q‖·‖d‖)，检索取 top-k 最大者',
        'desc': '稠密检索的标准打分：查询向量与文档向量做点积后各自归一化，'
                '等价于夹角余弦。实际系统常用内积近似加 ANN 索引（HNSW/IVF）加速，'
                '此时必须保证向量已归一化，否则内积会被模长带偏。',
    },
    'min-max': {
        'eq': 'x′ = (x − min)/(max − min)；[-1,1] 映射 x″ = 2x′ − 1；Z-score 近似 (x − x̄)/((max − min)/2)',
        'desc': 'Min-Max 把特征线性压到固定区间，保留原始分布形状但不改变相对关系；'
                '缺点是受离群点支配（一个极大值会把其余样本挤到窄区间）。'
                '树模型不需要缩放，而梯度下降类与距离类模型通常必须做。',
    },

    # ---- 神经网络基础 ----
    'sigmoid': {
        'eq': 'σ(z) = 1/(1 + e^(−z))；对数几率 logit = ln[p/(1 − p)] = z',
        'desc': 'Sigmoid 把任意实数压到 (0,1)，输出可直接当概率用；其导数 σ′=σ(1−σ)，'
                '在 |z| 较大时趋于 0，是深层网络梯度消失的根源之一，'
                '所以隐藏层现多改用 ReLU 系。阈值不必固定 0.5，应按业务代价调整。',
    },
    'softmax': {
        'eq': 'Pᵢ = e^{zᵢ} / Σⱼ e^{zⱼ}',
        'desc': 'Softmax 把 logits 变成和为 1 的概率分布，指数放大差距使最大值更突出。'
                '实现上通常先减去 max(z) 再取指数以避免上溢；'
                '输出概率并不等于模型置信度，未经校准的模型常过度自信。',
    },
    'softmax-2': {
        'eq': 'Pᵢ(T) = e^{zᵢ/T} / Σⱼ e^{zⱼ/T}；分布熵 H = −Σ Pᵢ ln Pᵢ',
        'desc': '温度 T 控制分布锐度：T→0 趋近 one-hot（更确定），T→∞ 趋近均匀分布。'
                '训练时用高温做知识蒸馏可暴露类别间关系；'
                '但温度缩放只改变锐度不改变排序，不能修复模型本身的排序错误。',
    },
    'temperature-scaling': {
        'eq': 'z′ = z/T（T>1 平滑分布，T<1 锐化分布）',
        'desc': '对 logits 统一除以温度是最简单的概率校准手段，T 在验证集上用 NLL 搜索得到，'
                '不改变 argmax 因此不影响准确率，只改善置信度的可靠性（ECE）。'
                '它只能校正整体过度/不足自信，无法修正逐类别偏差。',
    },
    'relu-leakyrelu': {
        'eq': 'ReLU = max(0, x)；LeakyReLU = x (x≥0) 否则 αx；ELU = x (x≥0) 否则 α(eˣ − 1)',
        'desc': 'ReLU 计算极快且正区间梯度恒为 1，但负区间硬置 0 会造成"死神经元"；'
                'LeakyReLU 用很小的 α（常 0.01）保留一丝负梯度；ELU 负区间平滑趋近 −α，'
                '输出均值更接近 0，收敛更快但含指数运算成本略高。',
    },
    'dropout': {
        'eq': '保留概率 p = 1 − rate；训练期望 E[x] = p·x；缩放因子 = 1/p（inverted dropout）',
        'desc': '训练时按 rate 随机置零并在保留分支乘 1/p，使期望与推理一致，'
                '推理阶段不做任何丢弃也不缩放。这相当于对大量稀疏子网络做隐式集成，'
                '抑制神经元共适应；rate 常取 0.1–0.5，过大反而欠拟合。',
    },
    'cross-entropy': {
        'eq': '单样本 L = −[y·ln p + (1 − y)·ln(1 − p)]；批量 L = 单样本 × n',
        'desc': '二分类交叉熵对 confident 且错误的预测给出极大惩罚（p→0 而 y=1 时趋于无穷），'
                '梯度形式简洁 (p − y)，配合 sigmoid 反向传播非常干净。'
                '实现时要注意对 p 做 eps 截断防止 log(0)。',
    },
    'ai-8': {
        'eq': 'w′ = w − lr·(grad + 2λw)，其中 2λw 为 L2 正则梯度；Δw = −lr·(grad + 2λw)',
        'desc': '带 L2（权重衰减）的 SGD 单步更新：每次先按梯度与衰减项的和反向走一小步。'
                '注意真正的 weight decay 应独立于梯度缩放施加，而 Adam 中直接把 2λw 加进梯度'
                '会被自适应学习率抵消，因此 AdamW 才把衰减挪到更新步。',
    },
    'l1-l2': {
        'eq': 'L1 = λ₁·Σ|wᵢ|；L2 = λ₂·Σwᵢ²；总惩罚 = L1 + L2',
        'desc': 'L1 在 0 处不可导且梯度恒为 ±λ，会把不重要权重精确压到 0，产生稀疏解可用于特征选择；'
                'L2 惩罚与权重成正比，越接近 0 推力越弱，只做均匀收缩、保留小权重，'
                '泛化通常更稳。二者混用即 Elastic Net。',
    },
    'l1-l2-regularization': {
        'eq': 'L1 范数 = Σ|wᵢ|；L2 范数 = √(Σwᵢ²)',
        'desc': '两个范数量化权重矩阵的整体规模：L1 反映绝对值总量，对少量大权重敏感；'
                'L2（Frobenius 范数）反映能量，常被直接写进损失作为惩罚项。'
                '监控范数随训练的变化能快速判断是否出现权重爆炸。',
    },
    'lr-decay': {
        'eq': 'lr(epoch) = lr₀ × γ^epoch（指数衰减，γ<1）',
        'desc': '指数衰减让学习率随轮数几何级下降，早期大步探索、后期小步精修，'
                '有利于跳出尖锐极小值。γ 常取 0.9–0.99 或按验证指标下降时再降（ReduceOnPlateau）；'
                '搭配 warmup 可避免训练初期大学习率导致发散。',
    },
    'lr-warmup': {
        'eq': '预热步数 = 总步数 × 比例；预热末 LR = 峰值 LR',
        'desc': '训练最初若干步把学习率从 0 线性升到峰值，给 Adam 的二阶动量估计留出稳定时间，'
                '避免大 batch 训练初期因梯度方差大而发散。'
                '典型配置是总步数的 1%–5%，之后接 cosine 或线性衰减。',
    },
    'grad-accumulation': {
        'eq': '累积步数 = ⌈目标批量 / (单卡批量 × 卡数)⌉',
        'desc': '显存不够放大 batch 时，用小批量连续跑若干步再把梯度相加、只做一次参数更新，'
                '等效于大批量训练。注意归一化层统计与 BatchNorm 仍按小批量计算，'
                '与真正的大 batch 不完全等价；学习率要按等效批量同步放大。',
    },
    'ai-5': {
        'eq': '每轮步数 = ⌈样本数 / 批量⌉；总步数 = 每轮步数 × 轮数；最后一批大小 = 样本数 mod 批量（余 0 则取批量）',
        'desc': '参数更新次数由"样本数/批量"决定而非轮数，直接决定训练时长与学习率调度横坐标。'
                '最后一批不满时需要决定是否丢弃（drop_last）：'
                '丢弃可让 BatchNorm 统计稳定，保留则能多用一点数据。',
    },
    'steps-per-epoch': {
        'eq': '每卡步数 = ⌈样本数 / 批量⌉；每步覆盖样本 = 批量 × 卡数',
        'desc': '多卡数据并行时，全局批量 = 单卡批量 × 卡数，每卡步数不变但每步实际看到的样本翻倍数等于卡数。'
                '因此扩卡后要保持等效训练强度，需按比例调大学习率或增加轮数。',
    },
    'epoch-time': {
        'eq': '总步数 = ⌈轮数 × 样本数 / 批量⌉；训练时长(小时) = 轮数 × 样本数 / 批量 / 速度 / 3600',
        'desc': '把步数除以单卡吞吐（样本/秒）再换算成小时，用于排期与算力预算评估。'
                '速度为经验值，实际受数据加载、混合精度、通信开销影响，'
                '多卡时还要乘上并行效率（通常 0.7–0.9）。',
    },

    # ---- 模型规模 / 算力 ----
    'ai-6': {
        'eq': '全连接参数 = in × out + out（偏置）；3×3 卷积参数 = in × out × 9 + out；'
              '显存 MB = 参数 × 4 / 1024 / 1024',
        'desc': '按层的权重矩阵规模累加估算参数量，再按每参数 4 字节（FP32）折算显存。'
                '这只是推理权重的下限，训练还要额外放梯度、优化器状态（Adam 再翻 2 倍）与激活值，'
                '实际占用通常是权重的 4–6 倍。',
    },
    'transformer-params': {
        'eq': '主体参数 ≈ 12·L·d²；嵌入参数 ≈ vocab × d',
        'desc': '每层含 Q/K/V/O 四个 d×d 矩阵（4d²）与两层 FFN（通常 4d 宽，共 8d²），合计 12d²，'
                '乘层数 L 即得主体规模；词表嵌入另计 vocab×d（若解绑则输出层再加一份）。'
                '实际模型还有 LayerNorm 等小量参数，估算值略偏小。',
    },
    'attention-head-dim': {
        'eq': '每头维度 = d / heads；单层权重参数 ≈ 4d² + 2d·ffn',
        'desc': '多头注意力把 d 维切成 heads 份并行计算，每头维度须整除，'
                '头数过多会让单头维度过小、表达能力下降。'
                '单层参数由注意力四矩阵 4d² 与 FFN 两层 2d·ffn 构成，是显存与算力的主要来源。',
    },
    'attention-flops': {
        'eq': '单层 FLOPs ≈ 2S²d (QKᵀ) + 2S²d (AV) = 4S²d；全模型 GFLOPs = 单层 × 层数 / 10⁹',
        'desc': '自注意力的两次矩阵乘各为 S×S×d 规模，乘加各算一次故乘 2，合计 4S²d，'
                '随序列长度呈平方增长——这正是长上下文成本暴涨的原因。'
                '线性注意力与稀疏注意力正是为打破这一 S² 项。',
    },
    'flops': {
        'eq': '卷积 FLOPs ≈ 2·H·W·C_in·C_out·K²；全连接 FLOPs ≈ 2·in·out；MACs ≈ FLOPs / 2',
        'desc': '按输出特征图每个元素所需的乘加次数累计：卷积为 K²·C_in 次乘加、输出 HWC_out 个元素，'
                '乘 2 得 FLOPs。1 MAC = 1 次乘加 = 2 FLOPs，硬件标称算力多用 MACs，'
                '对比时要注意单位换算。',
    },
    'training-flops': {
        'eq': '训练 FLOPs ≈ 6·N·D（N 为参数量，D 为训练 token 数）；'
              '训练月数 ≈ 6ND / (卡数 × 单卡算力 × 3600 × 24 × 30)',
        'desc': 'Transformer 训练的经验法则：前向 2N、反向约 4N，合计每 token 约 6N FLOPs，'
                '即 Chinchilla 系列缩放律推导的基础。'
                '再除以集群有效算力（须乘 MFU，实际常 30%–50%）得到训练周期。',
    },
    'vram-estimate': {
        'eq': '权重显存 ≈ 参数量 × 每参数字节数；总显存 ≈ 权重 + 激活/1000',
        'desc': '推理显存主要由权重决定：FP16 为 2 字节、INT8 为 1 字节、FP32 为 4 字节。'
                '训练还要加梯度与 Adam 状态（各再占 1 份与 2 份权重），'
                '加上随批量与序列长度增长的激活值，常是权重本身的数倍。',
    },
    'quantization-ratio': {
        'eq': '压缩比 = 原位数 / 目标位数；模型大小(MB) ≈ 参数量 × 位数 / 8 / 1024²',
        'desc': '量化通过降低权重数值精度（FP32→INT8 即 4:1）直接缩小体积与带宽需求，'
                '算力受限场景收益明显。但精度损失会体现在指标上，'
                '对异常值敏感的模型需做量化感知训练或逐通道校准。',
    },
    'inference-throughput': {
        'eq': '吞吐 = tokens / 秒；单请求吞吐 = tokens / (秒 × 并发批量)',
        'desc': '推理服务的核心指标：总吞吐衡量集群利用率，单请求吞吐才是用户感知的快慢。'
                'batching 能显著提升总吞吐但会拉长单请求延迟，'
                '在线服务通常需要在两者间按 SLO 折中。',
    },
    'multi-agent-overhead': {
        'eq': '消息总数 = n(n − 1) × 轮数；总流量 = 消息总数 × 单条大小',
        'desc': '全连接广播模式下每个智能体每轮要向其余 n−1 个发消息，'
                '通信量随智能体数量呈平方增长，这是多智能体系统规模化的主要瓶颈。'
                '改为分层或共享黑板（blackboard）可把复杂度降到线性。',
    },

    # ---- 训练诊断与数据处理 ----
    'ai-11': {
        'eq': '损失差距 = 验证损失 − 训练损失；准确率差距 = 训练准确率 − 验证准确率；'
              '风险判据：损失差 > 0.3 或准确率差 > 10%',
        'desc': '训练指标持续优于验证指标就是过拟合信号，差距越大越强。'
                '阈值 0.3 / 10% 是常用经验线，但不同任务量纲差异很大，'
                '更可靠的是看验证损失是否开始回升（应触发早停）。',
    },
    'ai-13': {
        'eq': '早停等待时间(分钟) = 耐心轮数 × 单轮时长 / 60；最大建议轮数 = 最小轮数 + 耐心 × 3',
        'desc': '早停用"连续多少轮验证指标未改善就停止"防止无效训练与过拟合。'
                '耐心值太小会在平台期提前终止（验证损失常先震荡后下降），'
                '太大则浪费算力；一般给 5–20 轮，并按单轮时长折算可接受的等待成本。',
    },
    'best-epoch': {
        'eq': '最佳轮次 = argmin(验证损失)；最小验证损失 = min(l₁…l₅)',
        'desc': '取验证损失最低的轮次而非最后一轮，是早停的标准做法。'
                '若多个轮次损失接近，优先选更靠后的（通常更稳）或按次要指标（F1）再筛；'
                '注意"用验证集选轮次"本身会轻微乐观，严格评估应留出独立测试集。',
    },
    'ai-12': {
        'eq': '理论扩充量 = 基数 × 增强倍数；有效等效量 = 基数 × [1 + (倍数 − 1) × 多样性系数]',
        'desc': '数据增强的收益取决于"新增样本带来多少新信息"：'
                '若变换高度相似（旋转 1°），多样性系数趋近 0，扩得再多也只是重复；'
                '系数接近 1 才算真正等效的新数据。有效量超过数千后边际收益迅速下降。',
    },
    'augmentation-multiplier': {
        'eq': '增强后总量 = 原始样本数 × (1 + 系数 × 变换种类数)',
        'desc': '按每种变换贡献的增量线性外推总量，系数反映单次变换的等效增益（通常 <1）。'
                '这是上界估计：变换间高度相关时实际有效量远小于此，'
                '且增强样本不能完全替代真实数据的分布覆盖。',
    },
    'elbow-wcss': {
        'eq': '降幅 = (WCSS_k − WCSS_{k+1}) / WCSS_k × 100%；比率 = WCSS_{k+1} / WCSS_k',
        'desc': '手肘法看簇内平方和随 K 增大而下降的边际收益：降幅突然变小的拐点即"手肘"。'
                '它依赖人眼判读，簇形状不均时拐点不明显，'
                '可辅以轮廓系数或 Calinski-Harabasz 指数交叉验证。',
    },
    'k-means': {
        'eq': 'Inertia 估算 ≈ n·dim·spread²/k；每簇样本 ≈ ⌈n/k⌉',
        'desc': '在样本近似均匀分布的假设下，簇内平方和随 K 增大按 1/k 衰减，'
                '可用于快速预判需要多少簇。实际数据有簇密度差异时，'
                '应结合业务含义定 K，而不是只看指标拐点。',
    },
    'ai-9': {
        'eq': '基尼 = 2p(1 − p)；加权子基尼 = w_L·2p_L(1 − p_L) + (1 − w_L)·2p_R(1 − p_R)；'
              '基尼增益 = 根基尼 − 加权子基尼；熵 = −p·log₂p − (1 − p)·log₂(1 − p)',
        'desc': '决策树分裂准则：基尼与熵都衡量节点纯度，取值越小越纯。'
                '基尼计算更快（无对数）是 CART 默认；熵对纯度变化略敏感。'
                '二者选出的分裂在绝大多数数据集上差异极小。',
    },
    'ai-15': {
        'eq': 'P(B) = 先验 × 敏感度 + (1 − 先验) × (1 − 特异度)；'
              '后验 P(A|B) = 先验 × 敏感度 / P(B)；'
              'NPV = (1 − 先验) × 特异度 / [(1 − 先验) × 特异度 + 先验 × (1 − 敏感度)]',
        'desc': '贝叶斯定理在检测场景的直接应用：即便试剂敏感度、特异度都很高，'
                '若疾病先验极低，阳性预测值仍可能很小（假阳性占多数）——'
                '这是"筛查阳性≠患病"的数学根据，也是高危人群筛查才有意义的理由。',
    },
    'ai-16': {
        'eq': '加权平均概率 = Σpᵢwᵢ / Σwᵢ；硬投票 = Σ[pᵢ ≥ 0.5] ≥ 2 判正类；'
              '集成置信度 = |加权概率 − 0.5| × 2',
        'desc': '软投票用权重平均概率，能利用模型输出的置信度信息，通常优于硬投票的多数决。'
                '权重可按各模型验证集表现分配；'
                '若子模型同质性太强（同一架构不同种子），集成增益会非常有限。',
    },
    'rag': {
        'eq': '召回率 = 检索到的相关文档数 / 相关文档总数 × 100%；'
              '精确率 = 检索到的相关文档数 / 检索总数 × 100%；'
              'F1 = 2·P·R/(P + R)；命中率 = 检索到 ≥1 篇相关 ? 100% : 0',
        'desc': 'RAG 的检索质量直接决定生成上限：召回率不足会漏掉关键证据导致幻觉，'
                '精确率不足则把噪声塞进上下文稀释注意力。'
                '命中率（Recall@k 的特例）常用于快速判断 top-k 是否够用。',
    },
    'ai-10': {
        'eq': '输出尺寸 = ⌊(W + 2P − K − (K − 1)(D − 1))/S⌋ + 1；'
              '感受野 = K + (K − 1)(D − 1)',
        'desc': '卷积输出尺寸由输入宽、卷积核、填充、步长与膨胀率共同决定，'
                '结果须为整数，非整除时多数框架向下取整（丢弃边缘）。'
                '膨胀卷积用 (K−1)(D−1) 扩大感受野而不增加参数，适合密集预测任务。',
    },

    # ---- Token 与上下文 ----
    'token': {
        'eq': '估算 token 数 = ⌈字符数 × 语言系数 / 重叠系数⌉；英文参考 = ⌈字符数 × 0.3 / 1.5⌉；'
              '成本 ≈ token 数 / 10⁶ × 单价',
        'desc': '不同语言与分词器的压缩率差异很大：中文常约 1–1.5 字符/token，英文约 4 字符/token。'
                '此式为量级估算，精确计费必须调用对应模型的官方 tokenizer；'
                '代码与数字因切分更碎，实际 token 数常高于纯文本估计。',
    },
    'token-2': {
        'eq': '输入费用 = 输入 tokens / 10⁶ × 输入单价；输出费用 = 输出 tokens / 10⁶ × 输出单价；'
              '人民币 ≈ 美元 × 汇率',
        'desc': '大模型 API 普遍按输入与输出分别计价，且输出单价通常高于输入，'
                '因此压缩提示词与控制生成长度都能直接省钱。'
                '换算人民币时汇率按当期取值，另需考虑批量/缓存折扣与最低计费粒度。',
    },
    'token-usage': {
        'eq': 'tokens ≈ 中文字数 / 1.5 + 英文词数 / 0.75 + 代码字符数',
        'desc': '混合内容按三类分别折算：中文按字符、英文按词、代码按字符（切分最碎）。'
                '这是粗略预算工具，实际值随分词器与符号密度浮动 ±20%，'
                '用于判断是否接近上下文上限时应留出安全余量。',
    },
    'ai-17': {
        'eq': '已用 tokens = 系统提示 + 历史 + 当前输入；剩余 = 上下文窗口 − 已用；'
              '占用率 = 已用 / 窗口 × 100%',
        'desc': '上下文窗口是硬性上限，输入加输出总和超限会被截断或报错。'
                '占用超过 90% 时应压缩历史、摘要旧对话或改用更长上下文模型；'
                '注意输出也要预留额度，不能把窗口填满。',
    },
    'context-window': {
        'eq': '占用率 = (系统提示 + 用户输入 + 预期回复) / 上下文窗口 × 100%；剩余 = 窗口 − 已用',
        'desc': '规划对话时把预期回复长度一并计入，避免生成到一半撞上窗口上限被截断。'
                '长窗口模型虽标称很大，但注意力成本随长度平方增长，'
                '且中段内容容易被忽略（lost-in-the-middle），并非越长越好。',
    },
}
