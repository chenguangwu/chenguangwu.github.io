#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""network 分类 4 键 deep-dive 真实化（GEN 占位 -> 真实网络工程内容）。
保留原 title，键数守恒 5022，indent=1 + separators=(",", ": ")。
"""
import json, sys, os

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
JSON_PATH = os.path.join(ROOT, "i18n", "tools", "content_deepdive.json")

NEW = {
    "network/convert-11": {
        "title": "IP地址转换（十进制 ↔ 二进制）",
        "scenarios": [
            "点分十进制与整数互转：把 192.168.1.1 这类可读地址转成 32 位整数（如 3232235777），便于数据库存储、排序或脚本批量处理。",
            "十进制与二进制互转：把 IPv4 写成 32 位二进制（每 8 位一个点），直观看掩码位、子网位与主机位。",
            "批量核对：对一批 IP 做互转并比对，避免录入错位（如多写少写一个段）。"
        ],
        "examples": [
            {
                "title": "转换示例",
                "body": "输入 192.168.1.1 → 十进制 3232235777；二进制 11000000.10101000.00000001.00000001（192=11000000、168=10101000、1=00000001）。反向：整数 3232235777 还原为 192.168.1.1；32 位串 11000000101010000000000100000001 还原同一地址。范围 0~4294967295（对应 0.0.0.0~255.255.255.255）。"
            }
        ],
        "faqs": [
            {
                "q": "为什么要有整数形式？",
                "a": "数据库常用 BIGINT 存 IP，整数便于索引、范围查询与去重；展示时再转回点分十进制。二进制则用于理解子网划分与掩码。"
            },
            {
                "q": "转换会出错吗？",
                "a": "纯位运算，只要输入合法 IPv4（四段 0~255）或 32 位 0/1 串即精确；段越界或长度不对工具会提示格式无效。"
            }
        ]
    },
    "network/calc-subnet": {
        "title": "子网掩码计算（CIDR / 子网划分）",
        "scenarios": [
            "CIDR 解析：输入 IP 与前缀（如 192.168.1.100/24），快速得到网络地址、广播地址、子网掩码与可用主机范围。",
            "地址规划：按部门/VLAN 拆分网段，根据主机数倒推所需前缀长度，避免地址浪费或不够用。",
            "排障核对：核对设备 IP 是否落在正确子网、网关与广播是否冲突。"
        ],
        "examples": [
            {
                "title": "子网划分示例",
                "body": "输入 192.168.1.100 / 前缀 24 → 网络地址 192.168.1.0、广播 192.168.1.255、子网掩码 255.255.255.0、通配符 0.0.0.255；总地址 256、可用主机 254（首 192.168.1.1 ~ 末 192.168.1.254）。若前缀改为 /26，则每段仅 64 地址、可用 62，适合小 VLAN 隔离。"
            }
        ],
        "faqs": [
            {
                "q": "可用主机为什么要减 2？",
                "a": "每个子网首尾分别是网络地址与广播地址，不能分配给主机；故可用数=2^(32-前缀)-2（/31 点对点链路例外为 2，/32 单主机为 0）。"
            },
            {
                "q": "前缀怎么选？",
                "a": "先估最大主机数 N，找最小前缀使 2^(32-前缀)-2 ≥ N；常见：/24 约 254 台、/26 约 62 台、/30 点对点 2 台。前缀越小网段越大。"
            }
        ]
    },
    "network/analysis-manager-1": {
        "title": "工具（分析/管理/监测）使用",
        "scenarios": [
            "工具使用时长统计：把若干内部工具/系统的周活跃时长（小时）录成序列，看均值与标准差，判断哪些被重度依赖、哪些长期闲置。",
            "监测指标横向对比：把多个服务的监控采样值（如响应耗时、错误数）录成序列，用中位数与极差定位明显劣化的节点。",
            "数据质量校验：对人工填报的管理指标做描述统计，用标准差判断填报是否分歧过大、需要统一口径。"
        ],
        "examples": [
            {
                "title": "工具活跃度对比",
                "body": "输入：5 个内部工具周活跃时长（小时）12, 18, 9, 22, 15 → n=5、总和 76、均值 15.20、中位数 15.00、最小值 9、最大值 22、极差 13.00、方差 20.56、标准差 4.53。均值 15.2 小时说明整体使用较高，极差 13 提示头部工具（22h）与尾部（9h）差距明显，可针对低活跃工具排查推广或下线。"
            }
        ],
        "faqs": [
            {
                "q": "统计对管理有什么用？",
                "a": "工具/系统的使用时长、响应耗时、错误数都是数值序列，描述统计帮你判断「集中水平」与「个体差异」；标准差大说明分化明显，宜分层运营而不是一刀切。"
            },
            {
                "q": "多少个样本够？",
                "a": "至少 2 个才有意义；管理对比通常 5~20 个对象，样本越多标准差越稳健。本工具仅做描述统计，不替代根因分析。"
            }
        ]
    },
    "network/analysis-66": {
        "title": "效果（监测/分析/优化）模型",
        "scenarios": [
            "优化前后效果对比：把某指标优化前的多次采样（如页面加载耗时秒）录成序列，看均值与标准差，量化改进幅度。",
            "多版本 A/B 指标评测：把 A/B 两组的转化或耗时采样录成序列，用中位数与极差判断哪版更稳。",
            "监测数据异常筛查：对长期监测序列做描述统计，用极差/标准差识别离群采样点。"
        ],
        "examples": [
            {
                "title": "加载耗时优化对比",
                "body": "输入：优化后 5 次页面加载耗时（秒）3.2, 2.8, 2.5, 2.1, 1.9 → n=5、总和 12.5、均值 2.50、中位数 2.50、最小值 1.9、最大值 3.2、极差 1.30、方差 0.22、标准差 0.47。均值降到 2.50 秒且标准差仅 0.47，说明优化后不仅更快且更稳定；若对照优化前均值 4.0 秒，则降耗约 37.5%。"
            }
        ],
        "faqs": [
            {
                "q": "怎么判断优化有效？",
                "a": "看均值是否显著下降（或指标上升）且标准差不增大；标准差小说明每次表现一致、不是偶发。建议保留优化前后两次采样序列做对比。"
            },
            {
                "q": "样本少可信吗？",
                "a": "5 次采样可做初步判断，但要下结论建议 20~30 次以上并排除离群点；本工具只给描述统计，显著性检验需另做。"
            }
        ]
    },
}

def main():
    with open(JSON_PATH, "r", encoding="utf-8") as f:
        data = json.load(f)
    before = len(data)
    missing = [k for k in NEW if k not in data]
    if missing:
        print("ERROR missing keys:", missing); sys.exit(1)
    for k, v in NEW.items():
        data[k] = v
    after = len(data)
    assert after == before, "键数变化 %d->%d" % (before, after)
    assert after == 5022, "键数断言失败: %d != 5022" % after
    with open(JSON_PATH, "w", encoding="utf-8") as f:
        json.dump(data, f, indent=1, ensure_ascii=False, separators=(",", ": "))
        f.write("\n")
    print("OK network 4 键已真实化，键数守恒=%d" % after)

if __name__ == "__main__":
    main()
