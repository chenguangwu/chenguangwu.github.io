#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""真实化 office 分类 7 条 deep-dive（替换 STY3 标准口径占位），键数守恒 5022。

office 工具均为纯前端文档/编辑型（Excel 公式速查、流程图、Markdown 编辑器、
思维导图、PDF 合并/旋转/拆分），无数值计算逻辑，故 examples 写真实操作示例。
"""
import json
import re

JSON_PATH = 'i18n/tools/content_deepdive.json'

GEN = r'当你在\w+相关场景中|在对应的输入框或选项中填写|快速复核|统一输入单位与口径|标准化后再执行对比|为何要生成可复核输出|在office场景里，优先把'


def load():
    with open(JSON_PATH, encoding='utf-8') as f:
        return json.load(f)


def save(d):
    with open(JSON_PATH, 'w', encoding='utf-8') as f:
        json.dump(d, f, ensure_ascii=False, indent=1, separators=(',', ': '))
        f.write('\n')


def set_dd(d, key, title, scenarios, examples, faqs):
    d[key] = {
        'title': title,
        'scenarios': scenarios,
        'examples': examples,
        'faqs': faqs,
    }


def main():
    d = load()
    before = len(d)

    # office/excel-formula-reference —— Excel 公式速查表
    set_dd(d, 'office/excel-formula-reference', 'Excel 公式速查表',
        [
            '撰写月度销售报表时，需要快速回忆 VLOOKUP、SUMIFS、IF 等常用函数的参数顺序与写法，直接在速查表搜索框输入函数名即可定位语法模板与示例。',
            '搭建数据看板前，对比 XLOOKUP 与 VLOOKUP 的适用场景，确认当前 Excel 版本是否支持动态数组函数，避免写错引用方式。',
            '给同事做培训时，把 SUMIFS 多条件求和、COUNTIFS 计数等高频公式的要点整理成速查条目，减少反复查帮助文档的成本。',
        ],
        [
            {'title': '示例：用 SUMIFS 统计指定地区销售额',
             'body': '假设 A 列为地区、B 列为销售额，统计「华东」地区总额：=SUMIFS(B:B, A:A, "华东")。速查表里可直接复制该语法模板，把区域与条件替换成自己的字段即可，无需记忆参数顺序。'},
        ],
        [
            {'q': '如何快速查找某个函数？', 'a': '在页面顶部搜索框输入函数名（如 VLOOKUP、IF、SUMIFS），即可筛选并高亮对应条目，不必翻阅长列表。'},
            {'q': '公式参数顺序记不清怎么办？', 'a': '每条公式都给出标准语法模板与参数说明，按模板填空即可；可选参数会单独标注，避免误填导致 #VALUE! 错误。'},
        ])

    # office/flowchart —— 流程图绘制工具（Mermaid）
    set_dd(d, 'office/flowchart', '流程图绘制工具',
        [
            '梳理业务流程或审批流时，用图形把「开始→判断→处理→结束」的节点和分支画清楚，便于向团队讲解与对齐。',
            '做技术方案时绘制接口调用时序图，把多个服务之间的请求与响应顺序可视化，减少沟通歧义。',
            '准备项目汇报材料时，用架构图/类图表达模块关系，一键切换七种图表类型后导出图片插入文档。',
        ],
        [
            {'title': '示例：用 Mermaid 语法画一张判断流程图',
             'body': '输入：\ngraph TD\n  A[开始] --> B{条件判断}\n  B -->|满足| C[执行处理]\n  B -->|不满足| D[结束]\n页面会实时渲染（500ms 防抖），确认无误后点「导出 SVG / PNG」即可得到矢量或位图。'},
        ],
        [
            {'q': '支持哪些图表类型？', 'a': '内置七种图表类型可一键切换，涵盖流程图、时序图、类图、状态图等常见结构，切换后自动按对应语法重新渲染。'},
            {'q': '绘制内容会丢失吗？', 'a': '编辑内容自动保存到浏览器本地，刷新页面不会丢失；导出时支持 SVG（矢量、可二次编辑）与 PNG（位图）两种格式。'},
        ])

    # office/markdown-editor —— Markdown 编辑器（分屏预览）
    set_dd(d, 'office/markdown-editor', 'Markdown 编辑器（分屏预览）',
        [
            '撰写技术文档或博客时，左侧编写、右侧实时预览，写完即所见即所得，省去反复切换渲染工具。',
            '排版时用 Ctrl+B 加粗、Ctrl+I 斜体等快捷键快速套用格式，比手写标记符号更高效。',
            '交付文档时一键导出 HTML 或 Markdown 文件，方便发布到支持 GFM 的平台或留作离线备份。',
        ],
        [
            {'title': '示例：一段带表格与任务列表的 Markdown',
             'body': '# 周报\n\n**重点工作**：完成接口联调。\n\n| 模块 | 状态 |\n| --- | --- |\n| 登录 | 已完成 |\n\n- [x] 需求评审\n- [ ] 上线验证\n\n左侧输入后右侧实时渲染，支持 GFM 表格、任务列表、删除线与 180+ 语言代码高亮；内容自动存入 localStorage。'},
        ],
        [
            {'q': '编辑内容会丢失吗？', 'a': '编辑内容自动保存到浏览器 localStorage，下次打开会自动恢复；清除浏览器数据才会丢失草稿，建议重要文档及时导出。'},
            {'q': '支持哪些 Markdown 语法？', 'a': '支持 GFM 表格、任务列表、删除线、代码语法高亮（180+ 语言）等常用语法，工具栏也可快捷插入标题、链接、图片等元素。'},
        ])

    # office/mindmap —— 思维导图编辑器
    set_dd(d, 'office/mindmap', '思维导图编辑器',
        [
            '做读书笔记或头脑风暴时，用 # 标题与 - 列表的缩进直接表达层级，右侧自动生成思维导图，比手动画节点更快。',
            '整理会议议题时，把中心主题放在一级、讨论点放在子级，缩进越深层级越深，结构一目了然。',
            '准备汇报大纲时，先把要点写成 Markdown 大纲再导出图片，配合缩放、拖拽、节点折叠展开做演示。',
        ],
        [
            {'title': '示例：用缩进语法写一张思维导图',
             'body': '# 产品规划\n## 用户增长\n- 渠道投放\n- 活动裂变\n## 体验优化\n- 性能\n- 交互\n\n左侧按缩进书写，右侧实时生成导图；滚轮缩放、拖拽平移、单击圆点可折叠/展开子节点。导出 PNG 前先点「适应屏幕」让内容完整显示。'},
        ],
        [
            {'q': '如何调整节点层级？', 'a': '用 # 表示标题层级、用 - 列表表示子节点，缩进越多层级越深；Tab 键可快速缩进调整从属关系。'},
            {'q': '怎么导出完整思维导图？', 'a': '支持导出 SVG 与 PNG；导出 PNG 前建议先点「适应屏幕」，确保所有内容落在画布内不被裁切，草稿保存在 localStorage。'},
        ])

    # office/pdf-merge —— PDF 合并
    set_dd(d, 'office/pdf-merge', 'PDF合并工具',
        [
            '把多份分散的合同、扫描件按顺序合并成一个文件，方便统一归档与发送。',
            '提交申报材料时，将封面、正文、附件按目录顺序拼合，避免页数错乱。',
            '整理会议纪要时，把多页图片型 PDF 合并为单文档，减少文件数量便于检索。',
        ],
        [
            {'title': '示例：把三份文件按指定顺序合并',
             'body': '依次添加「封面.pdf、正文.pdf、附件.pdf」，列表从上到下的顺序即合并后的页码顺序（序号 ①②③）。可用拖拽或上移/下移按钮调整；加密或损坏的文件会被跳过并提示，不影响其余文件。处理在浏览器本地完成，文件不上传服务器。'},
        ],
        [
            {'q': '合并后的页码顺序怎么定？', 'a': '合并顺序就是文件列表从上到下的顺序，序号①②③对应合并后的第 1、2、3 页；用拖拽或上移/下移可精准调整。'},
            {'q': '加密或损坏的 PDF 能合并吗？', 'a': '加密或损坏的文件会被自动跳过并给出提示，不影响其他正常文件合并；建议先解密或修复后再加入。'},
        ])

    # office/pdf-rotate —— PDF 页面旋转
    set_dd(d, 'office/pdf-rotate', 'PDF页面旋转工具',
        [
            '扫描件方向不统一（横竖交错）时，逐页或批量旋转到正确方向，便于阅读打印。',
            '批量处理一份全部倒装的文档时，用「全部旋转」模式一次性校正。',
            '个别页面方向异常时，用「单页旋转」单独微调，不影响其余页面。',
        ],
        [
            {'title': '示例：把横向扫描页调正',
             'body': '选中目标页面后点「左转 90°」或「右转 90°」，缩略图实时预览旋转效果并显示当前角度；支持单页、批量、全部三种模式，可一键重置为原始方向。旋转只改变页面方向，不修改文字与图片内容。'},
        ],
        [
            {'q': '旋转会改动原文内容吗？', 'a': '不会。旋转仅改变页面方向（视觉角度），文字、图片等内容保持不变，可随时一键重置回原始方向。'},
            {'q': '支持哪些旋转模式？', 'a': '提供单页、批量、全部三种模式，可逐页微调也可整体校正；大文件生成缩略图可能需数秒，请耐心等待。'},
        ])

    # office/pdf-split —— PDF 拆分
    set_dd(d, 'office/pdf-split', 'PDF 拆分工具',
        [
            '从一份大合同中提取签字页或特定章节，单独拆分出来发送。',
            '按「1-3, 5, 7-10」这样的页码范围快速截取若干不连续页。',
            '用可视化缩略图勾选方式，所见即所得地挑选要保留的页面。',
        ],
        [
            {'title': '示例：按页码范围拆分',
             'body': '在页码范围输入框填写「1-3, 5, 7-10」，工具会按该顺序提取对应页生成新 PDF；也可用缩略图直接勾选，支持一键全选 / 反选 / 奇偶页快捷选择。所有处理在浏览器本地完成，文件不上传服务器。'},
        ],
        [
            {'q': '如何按不连续页码拆分？', 'a': '在页码范围输入框用「1-3, 5, 7-10」格式描述：短横线表示连续区间、逗号分隔多个片段，提取顺序即为填写顺序。'},
            {'q': '加密的 PDF 能拆分吗？', 'a': '加密/密码保护的 PDF 可以读取页数与缩略图，但提取页面时可能受限；建议先去除保护再操作。50 页以上大文件生成缩略图可能需要数秒。'},
        ])

    after = len(d)
    assert after == before, f'键数变化: {before} -> {after}'

    save(d)

    # 占位自检：仅针对 office 本批 7 键，避免其他未处理分类的残留占位干扰
    office_blob = json.dumps({k: d[k] for k in d if k.startswith('office/')}, ensure_ascii=False)
    assert not re.search(GEN, office_blob), 'office 仍存在 STY3/GEN 占位!'
    print(f'OK: office 7 条 deep-dive 已真实化，键数守恒 = {after}')


if __name__ == '__main__':
    main()
