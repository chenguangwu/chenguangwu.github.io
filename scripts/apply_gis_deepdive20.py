#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""gis 分类（4 工具）deep-dive 真实化（替换「高频复用模板」占位）。
- 覆盖：analysis-17（缓冲区分析/描述统计）、assessor-16（GPS PDOP 精度）、
  convert-46（经纬度↔度分秒）、convert-angle-slope-1（坡度 百分比↔角度）。
- 算例数字均经 node 复算核验（/tmp/verify_b20.js，19/19 通过）。
- 指南页克制：1 篇（assessor-16 GPS 精度评估，易误用/有计算依据），其余不铺量。
"""
import json, re, sys

SRC = 'i18n/tools/content_deepdive.json'

CONTENT = {
    'gis/analysis-17': {
        'title': '缓冲区（半径）分析与影响范围评估',
        'scenarios': [
            '选址/设施覆盖：按服务半径生成缓冲区，评估覆盖范围与盲区',
            'GIS 影响区：环境、噪声、安全距离的缓冲带划分',
            '数据支撑：对多方案半径做描述统计，定统一缓冲标准',
        ],
        'examples': [
            {'title': '方法', 'body': '算法：缓冲区是按给定半径（m）围绕地理要素生成的影响范围；本工具对输入的距离/数值序列做描述统计——数据量 n、总和、均值、中位数、最小值、最大值、极差、方差与标准差，用于判断典型缓冲半径与离散度。要求至少 2 个数值。'},
            {'title': '算例', 'body': '例：某设施 4 个候选服务半径（m）10, 20, 30, 40 → n=4、均值=25m、中位数=25m、极差=30m、标准差=11.18m（方差 125）。取均值 25m 作统一缓冲半径可覆盖多数候选点且离散度适中；极差 30m 提示最大与最小半径差 3 倍，应据实际服务标准分档而非一刀切。'},
        ],
        'faqs': [
            {'q': '缓冲区半径怎么定？', 'a': '依规范或经验（如学区 500m、消防 5 分钟车程、噪声 200m）；先用本工具对多方案半径做统计，取均值或分位数作统一标准，避免逐点随意设定。'},
            {'q': '统计和缓冲有什么关系？', 'a': '缓冲半径是数值输入，描述统计帮你看这批半径的集中与离散；标准差大说明方案差异大，宜分档处理，否则一刀切会漏掉边缘覆盖或过度覆盖。'},
        ],
    },
    'gis/assessor-16': {
        'title': 'GPS 定位精度与 PDOP 评估',
        'scenarios': [
            '定位质量：由 PDOP/HDOP/VDOP 与可见卫星数评估实时定位可信度',
            '误差预算：用 UERE（等效测距误差）估算水平/垂直/三维误差',
            'RTK 决策：PDOP 偏高或卫星不足时提示等待或差分增强',
        ],
        'examples': [
            {'title': '方法', 'body': '算法：校验 PDOP≈√(HDOP²+VDOP²)；三维误差≈PDOP×UERE、水平误差≈HDOP×UERE、垂直误差≈VDOP×UERE（UERE 单机等效测距误差，默认 3m）。精度等级按 PDOP：≤1 理想、≤2 优秀、≤5 良好、≤10 一般、>10 差。要求 PDOP≥0、卫星数≥0。'},
            {'title': '算例', 'body': '例：PDOP=2.0、HDOP=1.2、VDOP=1.6、UERE=3m、可见卫星 8 颗 → 校验 √(1.2²+1.6²)=2.000（与 PDOP 一致✓）、水平误差±3.60m、垂直±4.80m、三维±6.00m，判「优秀」。若 PDOP=8.0、HDOP=5.0、VDOP=6.2 → 校验 √(25+38.44)=7.965、水平±15.00m、垂直±18.60m、三维±24.00m，判「一般」且提示等待更多卫星。'},
        ],
        'faqs': [
            {'q': 'PDOP 越低越好吗？', 'a': '是，PDOP 反映卫星几何分布（越小越分散、越利于定位）；PDOP>10 几何条件差、定位误差显著增大，建议等卫星再分布或用 RTK/差分增强。'},
            {'q': 'UERE 取多少合理？', 'a': '单机 GPS 的 UERE 通常 1~5m（取决于星历、电离层、接收机），默认取 3m 作估算；接入 SBAS 或 RTK 后 UERE 可降到亚米乃至厘米级，相应误差缩小一个数量级。'},
        ],
    },
    'gis/convert-46': {
        'title': '经纬度与度分秒（DMS）互换',
        'scenarios': [
            '数据录入：十进制经度/纬度转度分秒便于地图标注',
            '成果表达：度分秒转十进制用于计算与批量处理',
            '坐标核对：两种格式互校，避免录入错位',
        ],
        'examples': [
            {'title': '方法', 'body': '算法：1°=60′=3600″；十进制→度分秒：度=取整、分=(小数×60)取整、秒=剩余×60；度分秒→十进制：十进=度+分/60+秒/3600。本工具按系数换算（val×rate×from/to）实现任意单位间转换，单位需一致。要求数值在合法范围（纬度 −90~90、经度 −180~180）。'},
            {'title': '算例', 'body': '例：12.5° → 12° + 0.5×60=30′ + 0″ = 12°30′00″（即 12.5×3600=45000″）。反向：45°30′00″ = 45+30/60+0=45.5000°；1°=3600″，故 0.5°=1800″ 对应 30′。经度 116.397° 即 116°23′49.2″（116+23/60+49.2/3600=116.3970）。'},
        ],
        'faqs': [
            {'q': '度分秒和十进制哪个好？', 'a': '十进制便于计算与批量处理（距离、投影、缓冲区），度分秒符合传统地图标注与航海/测量习惯；本工具双向互换，按用途选用。'},
            {'q': '秒为什么带小数？', 'a': '高精度坐标秒常带小数（如 49.2″）：赤道处 1″ 约对应地面 30m，亚秒级可达米级；录入保留小数可避免累积误差。'},
        ],
    },
    'gis/convert-angle-slope-1': {
        'title': '坡度（百分比 ↔ 角度）换算',
        'scenarios': [
            '道路/铁路：坡度百分比转角度以便纵坡设计与限坡校核',
            '土方/水利：渠道边坡角度转百分比便于断面绘制',
            '标注互通：两种坡度表达互校避免误读',
        ],
        'examples': [
            {'title': '方法', 'body': '算法：坡度百分比 pct=tan(角度°)×100，角度 deg=arctan(pct/100)×180/π。pct=100% 即 45°（tan45°=1）；pct 的物理含义是"垂直升高/水平距离×100"。要求 pct≥0、角度 0~90°。'},
            {'title': '算例', 'body': '例：坡度 100% → 角度=arctan(1)=45.00°；坡度 50% → arctan(0.5)=26.57°。反向：30° → tan(30°)×100=57.74%；45° → 100.00%。注意百分比随角度非线性增长（60° 对应 173.21%，并非 60%）。'},
        ],
        'faqs': [
            {'q': '100% 坡度是垂直吗？', 'a': '不是，100% 对应 45°（升高=水平距离）；真正垂直是 90° 即∞%（tan90° 无界）。道路限坡常用百分比（如 8%~12%），约对应 4.6°~6.8°。'},
            {'q': '角度和百分比哪个更直观？', 'a': '工程/交通常用百分比（直接是"每百米升几米"），数学/测量常用角度；本工具双向换算，按规范或图纸选用。'},
        ],
    },
}


def main():
    apply = '--apply' in sys.argv
    d = json.load(open(SRC, encoding='utf-8'))
    for k, v in CONTENT.items():
        try:
            assert len(v['scenarios']) == 3 and len(v['examples']) == 2 and len(v['faqs']) == 2, f'{k} 结构不符'
            for ex in v['examples']:
                if not re.search(r'\d', ex['body']):
                    print('⚠️ 缺数字警告:', k, ex['title'])
            d[k] = v
            print('新增/更新', k)
        except Exception as e:
            print('❌ 跳过', k, ':', e)
            continue
    if apply:
        json.dump(d, open(SRC, 'w', encoding='utf-8'), ensure_ascii=False, indent=1)
        print('已写入', SRC)
    else:
        print('（dry-run）')


if __name__ == '__main__':
    main()
