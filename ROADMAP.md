# ToolBox 下一步发展规划（ROADMAP）— 遗留任务

> **清理说明（2026-08-26 实测盘点）**：原 5 项"未完成"经数据核查——命名规范化 953 个编号 URL 远超阈值，按"太多不做"跳过；空壳/标题重复/math 过载已清零或前提不成立；basename 重复 104 组因 URL 风险跳过；高频工具质量打磨已完成。**ROADMAP 全部遗留任务清零。**

---

## 已完成 · 高频工具质量打磨（2026-08-26）

- 给 30 个 B 级（convert / calculator 类）注入「📐 工作原理与说明」formula-box 真实面板（复用 `scripts/enhance_b_a.py`，幂等），含消防 `calc-1/2/3/4`（目录名 `fire-rescue` 与 json `industry=fire` 不一致，已修正定位）。
- 质量 A 4993 → **5023（100%）**，B → 0，C → 0。门禁 5/5 全绿。

---

## 已验证清零 / 不做的项（2026-08-26 实测）

- **命名规范化（calc-NNN 等编号 URL）**：实测编号命名工具页 = **953**（遍布 100+ 行业），按"太多不做"规则跳过；后续新工具坚持语义化命名（即"新规则"），不再回头迁移旧编号 URL。
- **空壳/占位二次确认**：`tools/` 下 5316 个 html 无 `<script>` 页面 = 0；C 级 = 0。已清。
- **跨行业重复治理（标题）**：重复标题 = 0。已清。
- **跨行业重复治理（文件名）**：basename 重复 104 组（202 文件），改名涉及 URL 重命名 + SEO 风险，跳过；后续新工具避免同名。
- **功能分类重构（math 过载）**：math cat 仅 302（6.0%），calculator(867)/reference(520)/engineer(480) 更大，不过载，前提不成立，无需做。

---

## 现状基线（实测，2026-08-26）

| 指标 | 实测值 |
|---|---:|
| 索引收录工具 | 5,023 |
| 行业目录 | 266 |
| 质量分级 | A 5,023（**100%**） / B 0 / C 0 |
| 跳转桩 | 14 |
| 指南 | 74 篇正文（+6 cluster 页） |
| 质量门禁 | 五道全绿 |
| CI | 已配置（`.github/workflows/quality-gates.yml`，历史全绿） |
| 搜索能力 | 汉字 / 英文 / 拼音（完整 + 首字母） |
| LICENSE | MIT（已加） |

> 注：i18n 状态（行业英文名覆盖、翻译填充）由 i18n agent 跟踪，不在本规划范围。
