# 2026-08-29 工作日志

## 立项 + 完成 P1 扩展：英文构建期全量预渲染
- **立项依据**：实测确认英文 `<title>` 预渲染已由 `EN_OVERRIDE`（5066 条）全量命中（5022/5025，DEV-PLAN 旧口径"489"过时）；核心缺口是 `meta description` 仍 96.6% 中文（4855 页），而 `EN_OVERRIDE` 每条带 `ed` 字段（英文描述，5022 条现成却没用上）。
- **改动 `_build.py` fix_tool_pages_seo（4.1a）**：`meta/og/twitter:description` 命中 `EN_OVERRIDE[slug]['ed']` 时构建期注入英文；已存在中文 description 的页也强制覆写（与 title 英文逻辑一致）。`esc_once` 防双重转义。
- **补 3 个新功能页英文 key**（晚于 gen_en_override 批量生成，故漏）：`it/rich-text-editor`、`it/safelink-decoder`、`marketing/xiaohongshu-counter` → title 预渲染全覆盖 5025。
- **实测覆盖**：工具页 `meta description` 英文 170→5021（+4851）；`<title>` 英文 5024+（293 个中文 title 全为分类页/guide 页，非本轮工具页范围）。
- **四道门禁全绿**：`_test_static.py` ✅、`_audit_links.py --check` 0 死链、`_audit_assets.py --check` 0、`scripts/verify_calc.js` ✅（注意脚本在 scripts/ 不在 js/，DEV-PLAN 旧路径需更）。
- 构建产物随提交一起交（5033 文件：_build.py + _en_override.json + json/ + tools/ 全量页 + sw.js + tools.json）。

## 索引提交 crontab 配置复核（20:14）
- 老板重启用 IndexNow(14:00) + Bing(16:00，从15:00挪到16:00)，GSC Sitemap/GSC 监控两条保持注释禁用。
- **发现 bug**：老板粘贴的 crontab 第一行 `*/30* * * *`（缺空格）→ git push 任务语法非法失效；croniter 实测确认非法，已提醒改回 `*/30 * * * *`。
- 已更新 MEMORY.md「索引提交 cron」段为当前启用状态（GSC 两条禁用）。

## 纯前端清理优化（E5，20:30 收尾）
- **探测（数据驱动）**：坏分类名→行业英文映射 266/266 零缺失（E1 已修）；记录类工具→真·记账/日记/记事类命中 **0 个**（首次关键词误命中"记忆游戏/音乐音名"，清洗后无）；重复工具→18 组同名中 **10 组确为真功能重复**（约11文件），其余8组同名不同功能（如竞品份额分析 vs 竞品广告分析）保留。
- **执行（老板拍板"直接删除"）**：删 10 组真重复中的 11 个编号副本 html（design 三渐变/generator-5、science 双计算器/calculator-calc、pet-age/lawn-mowing/convert-23 副本、color-blindness-sim 副本、procurement/pulmonology 的 calc-1/rater-12、idiom-solitaire 副本、rail-fence-generator 副本），保留语义化 slug 页（gradient.html/calculator.html 等）。
- **消除死链**：2 个 guide 页"去使用"链接改指向保留页；build 刷新 json/sitemap/per-industry。slug-en.json / _en_override.json 清理 11 孤儿条目。
- **四道门禁全绿**：冗余 slug 0 / 死链 0 / 资产 0 / lang 缺失 0 / 重复 id 0 / 公式回归 ALL OK。工具总数 5025→5014，A 级 100% 保持。
- commit `8b904739` push origin/master（163 文件）。`git diff --cached --short` 是错误写法，应为 `git diff --cached --name-only`；`--no-optional-locks` 仅对 status/diff 有效，不能用于 commit。

## 收尾探测：除 GSC 外主动任务全闭环（21:3x）
- 重探剩余非 GSC 长尾：①中文模式英文 h2 —— 全站 h2 无 CJK 4905 页中，剔掉 E4 已加 data-zh 的 4895 页 + 14 手工 data-i18n 页，余 170 页全为专业公式/术语（s²=Σ...、F=ma、Sharpe 等）保留英文正确，**0 个真需改**；②P3-1 候选池 8-24 已完成（候选池清空）；③guide 150 页社交标签全有。
- 现状基线刷新：5014 工具 / 266 行业 / 150 指南，A 级 100%。
- 结论：按老板 8-29「除 GSC 的都干」指令，E1-E5 + 清理 + 候选池 + 社交标签全部闭环；GSC 收录提升按 8-29 规则保持暂停。
- 按 8-28 约定清空 DEV-PLAN.md 为 0 字节（无活跃非 GSC 任务；GSC 边界已留 MEMORY.md）。

## E6 guide 页 UI 对齐工具页（22:xx, 41ea2d9f）
- 机制：工具页页头页尾由 js/common.js 运行时注入（buildUnifiedHeader 注入 .nav-top 固定顶栏含搜索+语言切换；injectUnifiedChrome 注入 .footer 深色页脚）；原 isTool 判定仅 /tools/ 或 .nav 元素，guide 页不触发→只有自己简陋 <header> 面包屑 + <footer> 版权。
- 三处改 common.js：① toolPageRootPrefix 加 /guides/→'../' ② isTool 加 `pathname.indexOf('/guides/')` ③ 注入前移除 guide 自带 `body>footer` 避免与 .footer 重复。
- 150 guide 页：head 引 `../css/common.css`（与工具页共用 .nav-top/.footer/.breadcrumb 样式，body padding-top:68px 防固定 nav-top 遮挡）；删页内 `header{}`/`footer{}` 内联冲突块；`<header>` 面包屑解包为独立 `<nav class="breadcrumb">`；删自带 `<footer>` 版权。
- 7 个 cluster-*/index 页原仅引 analytics.js、缺 common.js→补 `/js/common.js` 才触发注入（与普通 guide 页引用一致）。
- 验证：无头 Chrome(puppeteer-core+本地Chrome)加 request 拦截(abort 非 localhost)绕开沙盒出网 502；guide 全类(prototype/普通/cluster/index)与工具页渲染一致(navTop+footer 注入、旧 footer 0 重复、breadcrumb 保留、body padding-top 68px、无报错)。
- 四门禁全绿；SW 戳 2bd558f3a3；A 级 100%。

## GSC 优化(无数据方向)试点（22:4x, 1e051b92）
- 老板 22:38 主动提 GSC（解除 8-29 静默规则），批准「A 试点」：不用 GSC 查询 CSV，按推荐方向先优化一遍。
- 实测基线：5288 工具页 meta description 0 套话/0 短描述，技术 SEO 8-26 审计已 100% 覆盖零缺口 → 无数据可做的站内项已做完；唯一真活=内容深度(打掉模板化页过滤=收录极低根因) + 12 英文重复描述。
- 落地：① `_build.py` fix_tool_pages_seo 新增 step6 content-depth 注入——从 `i18n/tools/content_deepdive.json`(新建)读独有使用场景/示例/FAQ，幂等(先清旧块再注入；锚点优先`<!-- 注意事项区块 -->`否则`<!-- 相关工具 -->`；无数据页/重定向桩自动跳过)。② it/ 15 高频工具(jwt/base64/uuid/json/url/timestamp/cron/sha/md5/password/qr 等)事实性深度内容(非套话)。③ `_en_override.json` 12 个同行业工具英文标题+描述写具体功能彻底去重(原泛化 Logistics/Maintenance 等撞车)。
- 踩坑：深度块输出初漏 `<!-- TOOLBOX-DEEP-DIVE -->` marker→guard 失效致重复注入；改用「先清旧块再注入」强幂等修复；连跑两次 build 验证 15 页各 1 块、0 重复。
- 四门禁全绿(死链0/资产0/lang缺失0/重复id0/公式回归 ALL OK)；A 级 100%；commit 1e051b92 push master。
- 遗留决策：内容深度是否铺开(设计/材料/工程等高质量行业批量)待老板拍板；GSC CTR 精修仍等查询 CSV（B 方向）。

## meta description 批量修复（Bing "过短" 报告）
- 老板贴 Bing Webmaster Tools 报告：50 个 URL 的 meta description 过短，优先处理。
- 根因：构建脚本 `_build.py` 用 `seo_desc=(_en_desc or t['desc'] or t['name'])[:160]` 注入 description；带 `ed` 英文 override 的页面每次 build 强制用 `ed` 覆盖 description。原 `ed` 是英文模板套话（"X - free online tool..."），信息不足且被 Bing 判短。
- 关键 gotcha：只改 HTML 静态 description 没用——下次 build 会被 `ed` 冲掉。必须把高质量 description 写入 `i18n/tools/_en_override.json` 的 `ed` 字段才耐久。
- 处理：46 个页面（43 工具页 + 3 聚合页 home/embedded/telecom）重写为 150-160 字符英文 description（Bing 报告明确 150-160，此长度仅对英文成立；中文 150-160 字会被截断）。
  - 工具页：更新 `ed` + 同步 HTML 三处标签(description/og:description/twitter:description)。
  - 聚合页：在 `_build.py` 加 `CATEGORY_DESC_OVERRIDE` 字典（动态带入工具数），使 build 后保持耐久。
- 跳过 it/qrcode-generator.html（noindex 跳转桩）；tools/literature/* 整目录不存在（Bing 陈旧 URL，3 个无法修复）。
- 门禁全过：_test_static / _audit_links --check / _audit_assets --check 均 0 失败。build 幂等验证：home/index 与 life/timestamp 的 description 在 build 后保持不变。
- 已 commit + push (e1b59de0, 318 files)。

## 中文优先 meta 反转（老板确认 23:2x，推翻上文英文方向）
- **根因确认**：原"英文优先"构建——`<html lang="zh-CN">` 但 title/description/og 初始渲染全英文，中文 title 靠 `<meta name="title-zh">`+JS 切回，description 无中文版 meta、JS 也不切。→ 百度不跑 JS 抓到英文 meta；Google 跑 JS 后 title 变中文但 description 仍英文。目标用户以中文为主，SE0 拿不到中文描述。
- **老板决策**：AskUserQuestion 选「中文优先(推荐)」，把全站 meta 从英文优先反转成中文优先。
- **`_build.py` 改动**：
  - 4.0 块：初始 `<title>` 渲染中文（per-industry 字典 `zh-CN.title` 或 `t['name']`），英文标题存 `<meta name="title-en">`（注入到 `I18N_HREFLANG_MARKER` 之前，否则被 inject_hreflang 截断），og:title/twitter:title 跟随中文。删旧 `title-zh`。
  - 4.1a 块：初始 `description`/`og:description`/`twitter:description` 渲染中文（源 `extract_zh_desc`：per-industry 字典 `zh-CN.intro/desc` 优先，过滤 `${...}` 模板串与 `<`，兜底中文正文/h2/标题+固定后缀）；英文描述存 `<meta name="desc-en">`（注入 marker 前）。`re.sub` 替换串改 lambda 防 `bad escape \x`（中文描述含 `\` 被当正则转义）。
  - `extract_zh_desc` 新函数：从 `_ind_cache`（per-industry 字典）取 `zh-CN.intro`，非中文/过短则依次尝试含中文 `<p>`（过滤 `${`/`<`）、中文 h2、兜底字符串；解决"误抽 uuid/JS 模板串"。
  - 聚合页 `generate_category_indexes`：初始 title/description/og/twitter 全渲染中文（`desc_meta_zh`），英文存 `title-en`/`desc-en`；CollectionPage JSON-LD description 改中文。
- **`js/i18n.js` 改动**：`syncTitle()` 现 `en-US` 读 `title-en` 切英文、否则用 `<title>` 文本；新增 `syncDesc()`：`en-US` 读 `desc-en` 切英文、否则用缓存 `_descZh`（初始中文）；`init()`/`set()` 均调 `syncDesc()`。`init()` 开头缓存 `_descZh`。
- **效果**：5014 工具页 100% 写入中文 description；全站工具 HTML 中文 description 覆盖 94.5%、中文 title 覆盖 89%（余下为 per-industry 词典缺 slug 的数据缺口页，回退英文，属正常）。聚合页/首页 meta 全中文优先，英文供 en-US 切换。
- **门禁**：_test_static / _audit_links --check / _audit_assets --check 全 0 失败。SW 戳 d37c6cffe0。
- 待提交：本次改动含全站 meta 反转（约 5795 文件含 build 产物），commit + push master。
